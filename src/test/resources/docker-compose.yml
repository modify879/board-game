version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_DB=board_game
      - POSTGRES_USER=board
      - POSTGRES_PASSWORD=*game*
      - TZ=Asia/Seoul
    ports:
      - '15432:5432'

  redis:
    image: redis:7.2.3
    ports:
      - '16379:6379'